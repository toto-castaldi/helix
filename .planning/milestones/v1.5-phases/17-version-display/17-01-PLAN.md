---
phase: 17-version-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/live/pages/TabletDateSelect.tsx
  - src/landing/main.ts
  - src/landing/style.css
autonomous: true
requirements: [VDSP-01, VDSP-02, VDSP-03, VDSP-04]

must_haves:
  truths:
    - "Coach app displays milestone version (e.g. v1.5) in the user dropdown menu"
    - "Live tablet app displays milestone version in the date-select header"
    - "Landing page displays milestone version in the footer"
    - "Landing page has a clickable link to https://github.com/toto-castaldi/helix"
  artifacts:
    - path: "src/components/Layout.tsx"
      provides: "Version display in coach app user menu"
      contains: "VITE_APP_VERSION"
    - path: "src/live/pages/TabletDateSelect.tsx"
      provides: "Version display in live tablet header"
      contains: "VITE_APP_VERSION"
    - path: "src/landing/main.ts"
      provides: "Version display and GitHub link in landing footer"
      contains: "VITE_APP_VERSION"
    - path: "src/landing/style.css"
      provides: "Footer styling for landing page"
      contains: "footer"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "src/live/pages/TabletDateSelect.tsx"
      via: "VITE_APP_VERSION env var in live build step"
      pattern: "VITE_APP_VERSION"
    - from: ".github/workflows/deploy.yml"
      to: "src/landing/main.ts"
      via: "VITE_APP_VERSION env var in landing build step"
      pattern: "VITE_APP_VERSION"
---

<objective>
Add milestone version display to the live tablet app and landing page, plus a GitHub repository link on the landing page.

Purpose: Complete the v1.5 versioning initiative by showing the Helix version on every user-facing surface and linking to the source repository.

Output: Version text visible on all three apps (coach, live, landing) and GitHub link on landing page.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-cicd-pipeline-cleanup/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add version display to live tablet app header</name>
  <files>src/live/pages/TabletDateSelect.tsx</files>
  <action>
Add version display to the TabletDateSelect.tsx header (the main screen users see when opening the live tablet app).

1. Add `const APP_VERSION = import.meta.env.VITE_APP_VERSION || 'dev'` at the top of the file (after imports, same pattern as `src/components/Layout.tsx` line 7).

2. In the header section (around line 40-51), add a version text element. Place it in the right side of the header, before the user email span. Use a subtle style consistent with the dark theme:
   ```tsx
   <span className="text-xs text-gray-500">{APP_VERSION}</span>
   ```
   Insert it inside the existing `<div className="flex items-center gap-4">` that contains the email and logout button, as the first element (before the email span).

NOTE: Do NOT modify TabletLive.tsx -- that screen has a compact header with client strip bar and adding version text there would clutter the coaching interface. The date-select screen is the appropriate place since it's the "home" screen of the live app.

VDSP-01 note: The coach app already displays version in Layout.tsx user dropdown (line 86: `Versione {APP_VERSION}`). This was pre-existing code. Phase 16 ensured the correct milestone value flows through VITE_APP_VERSION. No changes needed for coach app.
  </action>
  <verify>
1. `grep -c 'VITE_APP_VERSION' src/live/pages/TabletDateSelect.tsx` outputs `1`
2. `grep 'APP_VERSION' src/live/pages/TabletDateSelect.tsx` shows the constant definition and usage
3. `npm run build:live` succeeds without errors
  </verify>
  <done>Live tablet date-select screen header shows the milestone version (e.g. "v1.5") in subtle gray text next to the user email.</done>
</task>

<task type="auto">
  <name>Task 2: Add footer with version and GitHub link to landing page</name>
  <files>src/landing/main.ts, src/landing/style.css</files>
  <action>
Add a footer section to the landing page that shows the version and a link to the GitHub repository.

**In `src/landing/main.ts`:**

1. Add `const APP_VERSION = import.meta.env.VITE_APP_VERSION || 'dev'` at the top of the file (after the import line).

2. In the `render()` function, add a footer section AFTER the features section closing `</section>` tag (before the closing backtick of the template literal). The footer should contain:
   - The version text
   - A GitHub link to `https://github.com/toto-castaldi/helix`
   - The footer content does NOT need translation (version and GitHub are universal)

   Add this HTML after the features `</section>`:
   ```html
   <!-- Footer -->
   <footer class="site-footer">
     <span class="footer-version">${APP_VERSION}</span>
     <span class="footer-separator">·</span>
     <a href="https://github.com/toto-castaldi/helix" target="_blank" rel="noopener noreferrer" class="footer-github">
       <svg viewBox="0 0 24 24" fill="currentColor" class="footer-github-icon"><path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/></svg>
       GitHub
     </a>
   </footer>
   ```

**In `src/landing/style.css`:**

3. Add footer styles at the end of the file (after the `.bottom-cta` section styles). The footer should be minimal, centered, with muted colors:

   ```css
   /* --- Site Footer --- */

   .site-footer {
     display: flex;
     align-items: center;
     justify-content: center;
     gap: 0.5rem;
     padding: 2rem 1.5rem 2.5rem;
     font-size: 0.8rem;
     color: #9ca3af;
   }

   .footer-version {
     font-weight: 500;
   }

   .footer-separator {
     color: #d1d5db;
   }

   .footer-github {
     display: inline-flex;
     align-items: center;
     gap: 0.35rem;
     color: #9ca3af;
     text-decoration: none;
     transition: color 0.2s ease;
   }

   .footer-github:hover {
     color: #6b7280;
   }

   .footer-github-icon {
     width: 16px;
     height: 16px;
   }
   ```
  </action>
  <verify>
1. `grep -c 'VITE_APP_VERSION' src/landing/main.ts` outputs `1`
2. `grep 'github.com/toto-castaldi/helix' src/landing/main.ts` shows the GitHub link
3. `grep 'site-footer' src/landing/style.css` shows the footer styles
4. `npm run build:landing` succeeds without errors
  </verify>
  <done>Landing page displays a footer with the milestone version (e.g. "v1.5") and a clickable GitHub link pointing to https://github.com/toto-castaldi/helix that opens in a new tab.</done>
</task>

</tasks>

<verification>
1. Coach app: `grep 'VITE_APP_VERSION' src/components/Layout.tsx` confirms existing version display (VDSP-01)
2. Live tablet: `grep 'VITE_APP_VERSION' src/live/pages/TabletDateSelect.tsx` confirms version display added (VDSP-02)
3. Landing page: `grep 'VITE_APP_VERSION' src/landing/main.ts` confirms version display added (VDSP-03)
4. GitHub link: `grep 'github.com/toto-castaldi/helix' src/landing/main.ts` confirms link present (VDSP-04)
5. All builds pass: `npm run build && npm run build:live && npm run build:landing` succeeds
</verification>

<success_criteria>
- All three apps display the milestone version (coach in user menu, live in date-select header, landing in footer)
- Landing page footer contains a clickable link to https://github.com/toto-castaldi/helix
- All three builds compile without errors
- No regressions in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/17-version-display/17-01-SUMMARY.md`
</output>
