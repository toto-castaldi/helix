---
phase: 18-security-dead-code-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/helix-mcp/index.ts
autonomous: true
requirements:
  - SEC-01

must_haves:
  truths:
    - "No Bearer token authentication path exists in the Edge Function"
    - "No .well-known endpoints respond to requests"
    - "No OAuth discovery or WWW-Authenticate headers are returned"
    - "API key authentication continues to work for all methods except initialize"
    - "initialize method works without authentication"
    - "Unauthenticated requests receive JSON-RPC error with setup hint"
    - "GET requests receive 405 Method Not Allowed"
  artifacts:
    - path: "supabase/functions/helix-mcp/index.ts"
      provides: "MCP server with API key-only auth, no OAuth code"
  key_links:
    - from: "supabase/functions/helix-mcp/index.ts"
      to: "authenticateRequest function"
      via: "API key path only"
      pattern: "X-Helix-API-Key"
    - from: "supabase/functions/helix-mcp/index.ts"
      to: "main handler"
      via: "initialize bypass before auth check"
      pattern: "method.*initialize"
---

<objective>
Remove all OAuth 2.1 dead code from the MCP Edge Function and clean up authentication to be API key-only with proper JSON-RPC error responses.

Purpose: SEC-01 requires removing ~179 lines of OAuth dead code from the Edge Function. This includes Bearer token handling in `authenticateRequest()`, the `.well-known` endpoint handlers, the OAuth discovery metadata function, the `unauthorizedWithOAuthHint()` helper, and the GET request handler that served OAuth-dependent flows. After removal, the auth path is simplified to API key only, `initialize` is allowed without auth, and all GET requests return 405.

Output: Clean Edge Function with API key-only authentication, ~179 fewer lines of code.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-security-dead-code-removal/18-CONTEXT.md
@.planning/phases/18-security-dead-code-removal/18-RESEARCH.md
@supabase/functions/helix-mcp/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove OAuth code from authenticateRequest and helper functions</name>
  <files>supabase/functions/helix-mcp/index.ts</files>
  <action>
In `supabase/functions/helix-mcp/index.ts`:

1. **Remove Bearer token fallback from `authenticateRequest()`** (lines ~94-129):
   - Delete the entire block that handles `Authorization` header with Bearer token
   - Keep the API key path intact (lines ~73-92): reads `X-Helix-API-Key` header, hashes with SHA-256, looks up in `coach_ai_settings`
   - The function should only handle API key auth and return `{ userId }` or `null`

2. **Delete `getProtectedResourceMetadata()` function** (lines ~2281-2318):
   - This function returns RFC 9728 Protected Resource Metadata JSON
   - Delete the entire function definition

3. **Delete `unauthorizedWithOAuthHint()` function** (lines ~2320-2342):
   - This function returns 401 with WWW-Authenticate header pointing to OAuth discovery
   - Delete the entire function definition

After these deletions, verify no other code references `getProtectedResourceMetadata` or `unauthorizedWithOAuthHint` -- if they are called elsewhere, those call sites will be updated in Task 2.
  </action>
  <verify>
    <automated>cd /home/toto/scm-projects/helix && ! grep -n "Bearer" supabase/functions/helix-mcp/index.ts && ! grep -n "getProtectedResourceMetadata" supabase/functions/helix-mcp/index.ts && ! grep -n "unauthorizedWithOAuthHint" supabase/functions/helix-mcp/index.ts && ! grep -n "Authorization" supabase/functions/helix-mcp/index.ts && echo "OAuth helpers removed"</automated>
    <manual>No Bearer token, OAuth metadata, or OAuth hint functions remain</manual>
  </verify>
  <done>authenticateRequest() handles only API key auth. getProtectedResourceMetadata() and unauthorizedWithOAuthHint() are deleted entirely.</done>
</task>

<task type="auto">
  <name>Task 2: Remove OAuth endpoints from main handler and add initialize bypass + GET 405</name>
  <files>supabase/functions/helix-mcp/index.ts</files>
  <action>
In the main `Deno.serve()` handler of `supabase/functions/helix-mcp/index.ts`:

1. **Remove GET `.well-known/oauth-protected-resource` handler** (lines ~2379-2386):
   - Delete the block that checks URL path for `.well-known/oauth-protected-resource` and returns metadata

2. **Remove GET `.well-known/oauth-authorization-server` handler** (lines ~2391-2414):
   - Delete the block that proxies OAuth authorization server metadata from Supabase Auth

3. **Replace GET non-wellknown handler** (lines ~2416-2462):
   - The current GET handler serves OAuth-dependent SSE stream discovery and health checks
   - Replace the entire GET handler section with a clean 405 response:
   ```typescript
   if (req.method === "GET") {
     return new Response(JSON.stringify({
       jsonrpc: "2.0",
       id: null,
       error: {
         code: -32000,
         message: "Method not allowed. Use POST for JSON-RPC requests."
       }
     }), {
       status: 405,
       headers: { ...corsHeaders, "Content-Type": "application/json", "Allow": "POST, OPTIONS" }
     })
   }
   ```

4. **Add initialize bypass before auth check**:
   - After body parsing (the JSON-RPC request body is parsed to get the `method` field), check if `method === "initialize"`
   - If it is `initialize`, handle it immediately WITHOUT calling `authenticateRequest()`
   - This ensures MCP clients can discover server info before providing credentials
   - Per user decision: "initialize method allowed without auth (clients can discover server info)"
   - Move/restructure the handler flow: parse body -> check initialize -> authenticate -> handle method

5. **Replace `unauthorizedWithOAuthHint()` call with JSON-RPC error**:
   - Where auth failure previously called `unauthorizedWithOAuthHint()` (line ~2476-2478), return instead:
   ```typescript
   return new Response(JSON.stringify({
     jsonrpc: "2.0",
     id: body?.id ?? null,
     error: {
       code: -32000,
       message: "Missing X-Helix-API-Key header. Set X-Helix-API-Key header. Generate key in Helix Settings."
     }
   }), {
     status: 401,
     headers: { ...corsHeaders, "Content-Type": "application/json" }
   })
   ```
   - When API key is present but invalid, the error message should be: "Invalid API key. Generate a new key in Helix Settings."
   - Per user decision: specific error messages that reveal the reason, with setup hint

6. **Verify no remaining OAuth references**:
   - Search for any remaining references to "oauth", "Bearer", "well-known", "WWW-Authenticate", "authorization_server"
   - Remove any orphaned variables or imports related to OAuth
  </action>
  <verify>
    <automated>cd /home/toto/scm-projects/helix && ! grep -in "oauth" supabase/functions/helix-mcp/index.ts && ! grep -in "well-known" supabase/functions/helix-mcp/index.ts && ! grep -in "WWW-Authenticate" supabase/functions/helix-mcp/index.ts && ! grep -in "authorization_server" supabase/functions/helix-mcp/index.ts && echo "All OAuth removed from Edge Function"</automated>
    <manual>No OAuth references remain in the Edge Function</manual>
  </verify>
  <done>All OAuth endpoints removed from main handler. GET requests return 405. initialize method works without auth. Auth failures return JSON-RPC errors with setup hints. No OAuth references remain in the Edge Function.</done>
</task>

</tasks>

<verification>
- `grep -in "oauth\|bearer\|well-known\|WWW-Authenticate\|authorization_server" supabase/functions/helix-mcp/index.ts` returns no results
- `grep -n "authenticateRequest" supabase/functions/helix-mcp/index.ts` shows the function exists but only handles API key
- `grep -n "initialize" supabase/functions/helix-mcp/index.ts` shows the bypass logic
- `grep -n "405\|Method not allowed" supabase/functions/helix-mcp/index.ts` shows GET handler returns 405
- The Edge Function file compiles without TypeScript errors (check via Deno if available, or by confirming no syntax issues)
</verification>

<success_criteria>
The MCP Edge Function has zero OAuth code remaining. Authentication is API key-only. The initialize method works without auth. GET requests return 405. Auth failures return informative JSON-RPC errors with setup hints. File is ~179 lines shorter than before.
</success_criteria>

<output>
After completion, create `.planning/phases/18-security-dead-code-removal/18-02-SUMMARY.md`
</output>
