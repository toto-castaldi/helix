---
phase: 18-security-dead-code-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/OAuthConsent.tsx
  - src/App.tsx
  - src/pages/Settings.tsx
  - CLAUDE.md
autonomous: true
requirements:
  - SEC-01

must_haves:
  truths:
    - "Frontend has no OAuth consent page or route"
    - "Settings page has no Claude Web / OAuth section"
    - "CLAUDE.md documents API key auth only with no OAuth references"
  artifacts:
    - path: "src/pages/OAuthConsent.tsx"
      provides: "DELETED - no longer exists"
    - path: "src/App.tsx"
      provides: "Routes without /oauth/consent"
      contains: "No OAuthConsent import"
    - path: "src/pages/Settings.tsx"
      provides: "Settings without Claude Web section"
    - path: "CLAUDE.md"
      provides: "Documentation with API key auth only"
      contains: "X-Helix-API-Key"
  key_links:
    - from: "src/App.tsx"
      to: "src/pages/OAuthConsent.tsx"
      via: "Route import removed"
      pattern: "No import.*OAuthConsent"
---

<objective>
Remove all OAuth 2.1 dead code from the frontend and update documentation to reflect API key-only authentication.

Purpose: SEC-01 requires removing all OAuth dead code. This plan handles the frontend and documentation side -- deleting the OAuth consent page, removing the route from App.tsx, removing the Claude Web section from Settings, and cleaning OAuth references from CLAUDE.md.

Output: Clean frontend with no OAuth artifacts, documentation reflecting API key-only auth.
</objective>

<execution_context>
@/home/toto/.claude/get-shit-done/workflows/execute-plan.md
@/home/toto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-security-dead-code-removal/18-CONTEXT.md
@.planning/phases/18-security-dead-code-removal/18-RESEARCH.md
@src/pages/OAuthConsent.tsx
@src/App.tsx
@src/pages/Settings.tsx
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete OAuthConsent page and remove route from App.tsx</name>
  <files>src/pages/OAuthConsent.tsx, src/App.tsx</files>
  <action>
1. Delete `src/pages/OAuthConsent.tsx` entirely (312 lines).

2. In `src/App.tsx`:
   - Remove the import of OAuthConsent (line 13: `import OAuthConsent from './pages/OAuthConsent'` or similar lazy import)
   - Remove the route for `/oauth/consent` (lines 25-26 area)
   - Keep all other routes intact

Verify no other files reference OAuthConsent by searching: `grep -r "OAuthConsent" src/`
  </action>
  <verify>
    <automated>cd /home/toto/scm-projects/helix && ! test -f src/pages/OAuthConsent.tsx && ! grep -r "OAuthConsent" src/ && ! grep -r "oauth/consent" src/ && npm run build 2>&1 | tail -5</automated>
    <manual>OAuthConsent.tsx deleted, no references remain, build succeeds</manual>
  </verify>
  <done>OAuthConsent.tsx is deleted, App.tsx has no OAuth route or import, build passes with no errors</done>
</task>

<task type="auto">
  <name>Task 2: Remove Claude Web section from Settings and clean CLAUDE.md OAuth references</name>
  <files>src/pages/Settings.tsx, CLAUDE.md</files>
  <action>
1. In `src/pages/Settings.tsx`:
   - Remove the "Claude Web (claude.ai)" section (approximately lines 172-189 per research)
   - This section contains instructions about OAuth 2.1, consent page, and Claude Web connector setup
   - Keep the MCP API Key section and all other settings intact
   - Ensure no dangling imports or references after removal

2. In `CLAUDE.md`:
   - Remove the "Configurazione Claude Web" subsection under MCP Server Integration that describes OAuth 2.1, consent page, Bearer token auth
   - Remove OAuth-related entries from "Supabase Edge Function Secrets" if any exist for OAuth
   - Remove references to `/oauth/consent` route
   - Remove mentions of "OAuth 2.1", "Bearer token", "RFC 9728", "WWW-Authenticate", "oauth-protected-resource"
   - Keep the "Autenticazione" section but ensure it only describes API Key method (remove the "2. OAuth 2.1 (Claude Web)" subsection)
   - Keep the "Configurazione Claude Desktop" section with API key example intact
   - Ensure the MCP Server section clearly states API key is the only auth method

Per user decision: "Update CLAUDE.md to remove all OAuth references (Claude Web section, consent page, OAuth secrets) -- keep only API key auth documentation"
  </action>
  <verify>
    <automated>cd /home/toto/scm-projects/helix && ! grep -i "oauth" src/pages/Settings.tsx && ! grep -i "claude web" src/pages/Settings.tsx && ! grep -i "oauth" CLAUDE.md && ! grep -i "bearer token" CLAUDE.md && ! grep "oauth/consent" CLAUDE.md && npm run build 2>&1 | tail -5</automated>
    <manual>Settings has no Claude Web section, CLAUDE.md has no OAuth references, build succeeds</manual>
  </verify>
  <done>Settings page shows only API key integration (no Claude Web section), CLAUDE.md documents only API key auth with no OAuth/Bearer/consent references, build passes</done>
</task>

</tasks>

<verification>
- `src/pages/OAuthConsent.tsx` does not exist
- No grep hits for "OAuthConsent", "oauth/consent", "OAuth", "Bearer token" in `src/` directory
- No grep hits for "oauth", "Bearer token", "consent" in CLAUDE.md
- `npm run build` succeeds without errors
- Settings page still renders correctly with MCP API key section
</verification>

<success_criteria>
All OAuth 2.1 artifacts removed from frontend and documentation. Build passes. No references to OAuth remain in src/ or CLAUDE.md.
</success_criteria>

<output>
After completion, create `.planning/phases/18-security-dead-code-removal/18-01-SUMMARY.md`
</output>
